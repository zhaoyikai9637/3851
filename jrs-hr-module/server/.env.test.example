# Copy to server/.env.test only after confirming a separate NEW empty test database.
NODE_ENV=test
INTEGRATION_MODE=standalone
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=jrs_hr_module_test_20260908
DB_USER=jrs_hr_module_local
DB_PASSWORD=replace_with_your_mysql_password
DB_WRITE_CONFIRMED=
SEED_PASSWORD=replace_with_a_local_test_password_12_chars_min
MAIL_MODE=preview
MAIL_ALLOW_SMTP=false

# Team owns sign-in. Leave blank until its contract exists; no automatic login.
# Absolute HTTP(S) login URL. Return URL parameters must be agreed with the team.
TEAM_LOGIN_URL=
# Local server-side adapter file relative to server/; exports createIdentityAdapter.
TEAM_AUTH_ADAPTER=
