# Current development state

The application uses MySQL as its only runtime data source. Browser storage is not used for HR records.

Current scope:

- Temporary local entry for development only
- Persistent notifications, templates, profile changes, photos and sent-email history
- Team-owned authentication adapter required before production
- SMTP remains opt-in and disabled by default
